{
    "request": {
        "method": "POST",
        "url": "/test",
        "bodyPatterns": [
            {"matchesJsonPath": "$.voucher[?(@.voucherLimit == '1')]"},
        	{
        	"matchesJsonPath": { "expression":"$.voucher.voucherMPs", "contains":"voucher.simo.full.percentage"}
        	
        	}
        ]
    },
    "response": {
        "status": 200,
        "bodyFileName": "test/getTest.json"
    }
}